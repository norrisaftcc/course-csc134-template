# Fixture — a block that is nothing but an elision

Asserts no content, so it cannot be a faithful view of anything. This is the
loophole the elision rule would otherwise open.

```cpp source=.github/scripts/selftest/markdown/code/clean.cpp
// ... the whole program ...
```
