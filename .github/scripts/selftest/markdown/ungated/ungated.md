# Fixture — source= points outside the trees the compile gate covers

Matching this would prove the listing faithful to a file nothing compiles, which
is the appearance of a gate without the substance of one.

```cpp source=.github/scripts/selftest/must-warn.cpp
int main() { return 0; }
```
