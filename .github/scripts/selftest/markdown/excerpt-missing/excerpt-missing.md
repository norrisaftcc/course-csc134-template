# Fixture — an excerpt that does not appear in its source

Plausible-looking C++ that was never in the file. Exactly what an independently
authored listing looks like.

```cpp excerpt=.github/scripts/selftest/markdown/code/clean.cpp
    if (strength >= 40)
    {
        cout << "Borderline. Answer me this.\n";
    }
```
