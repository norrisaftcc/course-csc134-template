# Fixture — an invented gate= verb must be rejected, not tolerated

There is no skip. A gate that quietly ignores annotations it does not recognise
is a gate you can turn off by typo.

```cpp gate=skip reason="I would rather not"
int main() { return 0; }
```
